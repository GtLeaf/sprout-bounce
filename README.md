# 芽芽弹跳方阵

完整开发包已内置 Three.js 和全部运行资源，不需要联网加载依赖。

## 启动与开发

- Windows 试玩：双击 `start-game.cmd`，浏览器会自动打开游戏。
- 命令行启动：安装 Node.js 18 或更高版本后运行 `npm start`，再访问终端显示的本地地址。
- 自动测试：运行 `npm test`。
- 发布：将 `index.html`、`game.js`、`game-rules.mjs`、`style.css`、`assets` 和 `vendor` 原样部署到任意静态网站空间。
- 项目没有构建步骤和线上接口；声音由浏览器实时生成，存档、账号、支付和服务端功能尚未接入。

## 操作

- 电脑：使用 `WASD` 或方向键跳到相邻方块，也可以点击相邻方块。
- 手机：在棋盘任意位置滑动；也可以使用右下角四向键。轻触移动一格，按住同一方向连续跳，手指滑出按键或松手立即停止。
- 角色只能向上、下、左、右移动，不能斜向移动。

## 规则

- 完整棋盘为 7×7，方块依次按橙、黄、绿、蓝、粉、红循环变色。
- 每次落地只会把当前方块推进到下一种颜色。
- 四个或更多同色方块只要通过上、下、左、右连通就会进入爆炸倒计时；直线、L 形、T 形和块状都有效，对角线接触不算连通。
- 爆炸只作用于本次落地形成的同一片连通组合，不会波及旁边独立的同色方块。
- 玩家可以停在任意稳定方块上；爆炸瞬间仍在危险组合中会损失一条生命，并在安全方块复位，三条生命耗尽才会结束本局。
- 爆炸后会露出凹陷底盘，空位依次消费左上角“下一组”的颜色并从上方补回。
- 新补回的同色方块不会自动触发消除，必须由玩家再次落地变色后检查。
- 每次爆炸组合计入一次 `COMBO`；在限时内达到本层目标后进入下一层。
- 达到目标后会立即冻结关卡计时，但不会锁住移动；玩家仍可躲避尚未结束的爆破，待方块全部补回后再进入下一层。
- 更大的爆破组合会提高奖励等级；奖励依次为金币、蓝宝石、红宝石、祖母绿、钻石和金条。

## 美术与声音

- 游戏内主角已经改为实时受光的程序化 3D 低多边形模型，不再使用始终朝向镜头的 2D Sprite；身体、叶片、五官、脚和棋盘共用同一套光照与阴影。
- `assets/sprout-island-arena.png`：与主角统一的浮岛游戏场景，任务 ID `631759099108790168`。
- `assets/sprout-keyart.png`：浮岛版开场主视觉，任务 ID `631759099109117848`。
- `assets/sprout-gameplay-style-guide.png`：本轮用于统一角色、棋盘、奖励与危险提示的玩法基准图，AIGC 任务 ID `631761202720018328`。
- `assets/sprout-ui-neutral-paper.png`：用于 HUD 与结算面板的极淡暖白纸纤维，AIGC 任务 ID `631773339743948696`；不含叶片、圆点、贴纸、条纹、边框或阴影，项目内压缩为 512×512。
- `assets/ui-sound-on.png`、`ui-sound-off.png`、`ui-restart.png`、`ui-forward.png`：原创暖白生活模拟类控件图标，由统一 2×2 母版裁切并透明化，AIGC 任务 ID `631778994806636798`；不复刻任何现有作品的图标或商标。
- `assets/sprout-logo-v50.png`：开场页原创扁平中文标题 Logo。以用户字体图提炼粗黑体骨架、几何切角、笔画粗细对比和跳跃节奏，以旧 Logo 保留文字、五色体系与主副标题层级；没有复制参考图的具体字形。AIGC 任务 ID `631785809275568382`，选中候选经五色纯色化、透明化与 `1200×580` 横版裁切后用于游戏。
- 主角、场景、棋盘和界面统一为明亮低多边形风格：几何色块、哑光表面、轻量明暗和清晰的小尺寸轮廓。
- 界面使用克制的生活模拟游戏语言：大面积暖白平面、少量青绿色状态色、柔和文字和充足留白；控件不使用外描边、硬投影、贴纸纹理或高饱和装饰，也不复制参考作品的角色、Logo 或具体 UI。
- 跳跃、连跳、落地、奖励、预警、塌陷、补块和结算音效由 Web Audio 实时分层合成。BGM 是 118 BPM 的原创轻盈玩具箱循环，移除规律低频鼓点，以高频玩具音、慢起音和错位弱拍为主；角色连续移动只播放一次软弹反馈，不再叠加完整的落地与起跳瞬态。爆破音采用软胶低频、玩具弹跳和晶亮碎片三层原创合成，不使用参考作品音频。
- 原作资源只用于玩法分析，没有把原作图片、模型或音频复制进当前游戏；新增奖励继续使用程序化 3D 几何体。

## 本轮修复

- 手机端相机改为左右对称并略微提高俯角，棋盘更接近正面俯视，同时仍保留方块厚度和跳跃高度。
- 声音按钮在桌面、平板和手机断点下都与相邻“下一组”底板等高；451×912 手机尺寸下二者上下边缘与中心线完全对齐。
- 结束或重开时会清理延迟奖励与弹窗任务，避免跨局残留。
- 不再允许向已经塌陷、正在掉落或正在补回的格子起跳；棋盘边缘也不会误跳出界。
- 初始棋盘随机生成后保证不会自带四方向连通的四格同色组合。
- 玩家被爆炸命中时，危险格与角色会一起下落并扣除一条生命，随后在安全格复位；关卡、分数和剩余时间不会重置。
- 手机滑动和右下角四向键都支持连续跳；四个方向使用独立触控区域，按键间隙与中心空白不会触发移动，降低误触和误换向。
- 四连点燃后会播放逐渐加速的短促提示音，爆破前加入短暂蓄力停顿；危险格按中心向外的波纹顺序弹落，并随链反层数增强冲击波、音高和镜头反馈。
- 配乐改为 8 小节轻盈错拍循环，低频在 210Hz 以下做了清理，并取消固定 kick；倒计时、掉落、扣除生命、单层完成、时间耗尽、最终失败与全部通关都有独立反馈音，关闭声音、重新开启和结束游戏时会平滑淡入淡出。
- 顶部只保留关卡、分数、时间、下一组和声音；品牌牌、装饰图标、叶片和贴纸全部移除。状态栏、进度、提示、移动键和结算统一为无外描边、无硬投影的暖白平面，选中态只使用低饱和青绿色；手机端保留下一组，并适配 320px 窄屏。
- 顶栏改为按同一条垂直中线排布；声音图标按钮清除浏览器默认内边距，在 320px 窄屏下与状态栏及“下一组”控件严格居中。
- 连通规则位于 `game-rules.mjs`。只有本次落地形成的四格及以上正交连通组会塌陷；独立同色格、纯对角线和新补回的格子都不会被连带点燃。自动测试覆盖直线、2×2、L/T 形、少于四格、棋盘边界和补格状态。
